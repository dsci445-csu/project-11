---
title: "Predicting AirBnB Rental Rates"
author: "Trevor Isaacson, Jonathan Olavarria, Jasmine DeMeyer"
date: "12/10/2021"
output: pdf_document
---

```{r setup, include=FALSE, echo = FALSE}
knitr::opts_chunk$set(echo = FALSE)
#knitr::opts_chunk$set(warning = FALSE)
library(ggplot2)
library(tidyverse)
library(tidyr)
library(e1071)
library(rmarkdown)
library(glmnet)

```

```{r}
training_df = read.csv("train.csv")
```

```{r}
training_df = training_df %>% 
  select(-c(thumbnail_url, name, X)) %>%
  mutate(price = exp(log_price))

head(training_df)
```
**Variable Descriptions:**
```{r}
training_df %>%
  group_by(property_type) %>%
  summarise(Total = n()) %>%
  arrange(desc(Total))

ggplot(data = training_df) +
  geom_histogram(aes(log_price), bins = 50) +
  ggtitle("Log Price")

ggplot(data = training_df) +
  geom_histogram(aes(price), bins = 100) +
  ggtitle("Listing Price ($)")



```









